---
import Layout from "../layouts/Layout.astro";
import ProperManagement from "../components/sections/ProperManagement.astro";

import {
  getLangFromUrl,
  useTranslations,
} from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

---


<Layout title=`${t("nav.manage")} - BR Solutions`>
  <ProperManagement />
</Layout>
