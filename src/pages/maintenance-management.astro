---
import Layout from "../layouts/Layout.astro";


import {
  getLangFromUrl,
  useTranslations,
} from "../i18n/utils";
import Maintenance from "../components/sections/Maintenance.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

---


<Layout title=`${t("nav.main")} - BR Solutions`>
  <Maintenance />
</Layout>
