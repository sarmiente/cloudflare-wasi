---
import Layout from "../../layouts/Layout.astro";
import Maintenance from "../../components/sections/Maintenance.astro";

import {
  getLangFromUrl,
  useTranslations,
} from "../../i18n/utils";


const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

---


<Layout title={t("nav.main")}>
  <Maintenance />
</Layout>
