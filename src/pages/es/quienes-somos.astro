---
import Layout from "../../layouts/Layout.astro";
import About from "../../components/sections/About.astro"

import {
  getLangFromUrl,
  useTranslations,
} from "../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={t("nav.who")}> 
  <About />
</Layout>